@* File Path: SeminarBookingSystem/Pages/UserManagements/UserManagement.cshtml *@
@page "/UserManagement"
@model UserManagementModel
@{
    Layout = "_MainLayout";
    ViewData["Title"] = "User Management";
}
<link rel="stylesheet" href="~/css/user-management.css" asp-append-version="true" />

<div class="management-container">
    <header class="page-header">
        <div class="header-text">
            <h1>User Management</h1>
            <p>Manage admin users and permissions</p>
        </div>
        <button class="btn-primary open-modal">+ Add Admin User</button>
    </header>

    <div class="alert-box">
        <i class="bi bi-shield"></i>
        <div class="alert-content">
            <strong>Super Admin Privileges</strong>
            <p>
                Only Super Admins can create, edit, and delete admin users. All new users will have the default password:
                <code class="pwd-highlight">admin123</code>
            </p>
        </div>
    </div>

    <div class="filter-bar">
        <div class="search-wrapper">
            <i class="fa-solid fa-magnifying-glass"></i>
            <input type="text" placeholder="Search users...">
        </div>
        <select class="role-select">
            <option>All Roles</option>
            <option>Super Admin</option>
            <option>Staff Admin</option>
        </select>
    </div>

    <div class="table-card">
        <div class="table-header">
            <h3>Admin Users</h3>
            <p>1 user</p>
        </div>
        <table class="user-table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Role</th>
                    <th>Created At</th>
                    <th class="text-right">Actions</th>
                </tr>
            </thead>
            @* <tbody>
                <tr>
                    <td>
                        <div class="user-name">
                            <i class="bi bi-shield"></i>
                            Super Admin
                            <span class="badge-you">You</span>
                        </div>
                    </td>
                    <td>admin@seminar.com</td>
                    <td><span class="role-badge">super admin</span></td>
                    <td>Mar 17, 2026</td>
                    <td class="text-right actions">
                        <button class="icon-btn edit"><i class="fa-regular fa-pen-to-square"></i></button>
                        <button class="icon-btn delete"><i class="fa-regular fa-trash-can"></i></button>
                    </td>
                </tr>
            </tbody> *@
            <tbody>
                @foreach (var user in Model.Users)
                {
                    <tr>
                        <td>
                            <div class="user-name">
                                <i class="bi bi-shield"></i>
                                @user.FullName
                            </div>
                        </td>
                        <td>@user.Email</td>
                        <td>
                            <span class="role-badge">
                                @Model.UserRoles[user.Id]
                            </span>
                        </td>
                        <td>@user.CreatedAt.ToString("MMM dd, yyyy")</td>
                        <td class="text-right actions">
                            <button class="icon-btn edit">
                                <i class="fa-regular fa-pen-to-square"></i>
                            </button>
                            <button class="icon-btn delete">
                                <i class="fa-regular fa-trash-can"></i>
                            </button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="summary-grid">
        <div class="summary-card">
            <div class="card-info">
                <p>Super Admins</p>
                <span class="count">1</span>
            </div>
            <i class="bi bi-shield"></i>
        </div>
        <div class="summary-card">
            <div class="card-info">
                <p>Staff Admins</p>
                <span class="count blue">0</span>
            </div>
            <i class="fa-solid fa-users-gear settings-icon"></i>
        </div>
    </div>
</div>
<!-- MODAL -->
<div class="modal" id="adminModal">
    <div class="modal-content">

        <button class="close-btn">&times;</button>

        <h2>Add Admin User</h2>
        <p class="subtitle">Create a new admin account</p>

        <form method="post" asp-page-handler="OnPostCreateAdminAsync">
            <div class="form-group">
                <label>Full Name <span>*</span></label>
                <input type="text" name="Input.FullName" placeholder="John Doe" required>
            </div>

            <div class="form-group">
                <label>Email <span>*</span></label>
                <input type="email" name="Input.Email" placeholder="john@seminar.com" required>
            </div>

            <div class="form-group">
                <label>Role <span>*</span></label>
                <select name="Input.Role" required>
                    <option value="Staff Admin">Staff Admin</option>
                    <option value="Super Admin">Super Admin</option>
                </select>
            </div>

            <div class="note-box">
                <strong>Note:</strong>The default password for new users is <strong>admin123</strong>. Ask the user to change it after first login.
            </div>

            <div class="footer">
                <button type="button" class="btn btn-cancel">Cancel</button>
                <button type="submit" class="btn btn-create">Create User</button>
            </div>
        </form>

    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
      const modal = document.getElementById("adminModal");
      const openBtn = document.querySelector(".open-modal"); // ✅ FIXED
      const closeBtn = document.querySelector(".close-btn");
      const cancelBtn = document.querySelector(".btn-cancel");

      if (!openBtn) {
        console.log("❌ open button not found");
        return;
      }

      openBtn.addEventListener("click", () => {
        modal.classList.add("show");
      });

      closeBtn.addEventListener("click", () => {
        modal.classList.remove("show");
      });

      cancelBtn.addEventListener("click", () => {
        modal.classList.remove("show");
      });

      window.addEventListener("click", (e) => {
        if (e.target === modal) {
          modal.classList.remove("show");
        }
      });
    });
</script>